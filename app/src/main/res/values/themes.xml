<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Reky" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/big_stone</item>
        <item name="colorPrimaryVariant">@color/big_stone</item>
        <item name="colorOnPrimary">@color/big_stone</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/big_stone</item>
        <item name="colorSecondaryVariant">@color/big_stone</item>
        <item name="colorOnSecondary">@color/big_stone</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Reky.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Reky.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Reky.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>